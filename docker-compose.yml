version: '3.3'

services:
  landing:
    build:
      dockerfile: ./Dockerfile.landing
      context: .
    restart: always
    ports:
      - "83:3000"
  app:
    build:
      dockerfile: ./Dockerfile.app
      context: .
    restart: always
    ports:
      - "89:3000"